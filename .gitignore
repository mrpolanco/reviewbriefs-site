# Dependencies (for future Laravel migration)
/vendor/
node_modules/

# Environment files
.env
.env.backup
.env.production

# Laravel (pre-emptive, for migration)
bootstrap/compiled.php
app/storage/
public/storage
public/hot
storage/*.key
.phpunit.result.cache
auth.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.sublime-workspace

# Build artifacts
/public/build
npm-debug.log
yarn-error.log

# Temp / local notes
scratch.*
_notes/
TODO.local.md

# ── Proprietary methodology (lives in reviewbriefs-ops private repo) ──
# Prompts are core IP — never expose in public repo
prompts/

# Analysis schema and worked examples reveal methodology
data/brief-schema.json
data/analysis-example.json

# Internal brief template reveals operator-only workflow
briefs/internal/
briefs/README.md

# Internal workflow documentation
CLAUDE.md

# Generated client briefs (never public — password-gated but still)
briefs/client/*/index.html
